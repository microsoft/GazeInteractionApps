<Page
    x:Class="TwoZeroFourEight.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TwoZeroFourEight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    KeyUp="OnPageKeyUp">

    <Page.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Width" Value="250" />
            <Setter Property="Height" Value="250" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="FontSize" Value="64" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        </Style>

        <local:IntegerToStringConverter x:Key="IntegerToStringConverter" />

    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="250" />
            <RowDefinition Height="*" />
            <RowDefinition Height="250" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Style="{StaticResource ButtonStyle}" Click="OnNewGame" Content="&#xE8F4;"/>
        <Button Grid.Row="0" Grid.Column="1" Style="{StaticResource ButtonStyle}" Click="OnUpClick" Content="&#xF0AD;"/>
        <TextBlock Grid.Row="0" Grid.Column="2" Text="{x:Bind Score, Mode=OneWay}"/>
        <Button Grid.Row="1" Grid.Column="0" Style="{StaticResource ButtonStyle}" Click="OnLeftClick" Content="&#xF0B0;"/>
        <Button Grid.Row="1" Grid.Column="2" Style="{StaticResource ButtonStyle}" Click="OnRightClick" Content="&#xF0AF;"/>
        <Button Grid.Row="2" Grid.Column="1" Style="{StaticResource ButtonStyle}" Click="OnDownClick" Content="&#xF0AE;"/>
        <TextBlock Grid.Row="2" Grid.Column="2" Text="Game Over" Visibility="{x:Bind GameOverStatus, Mode=OneWay}"/>

        <GridView Grid.Row="1" Grid.Column="1" ItemsSource="{x:Bind _board, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="local:Cell">
                    <Border BorderBrush="DarkGray" BorderThickness="2">
                        <TextBlock Text="{Binding Converter={StaticResource IntegerToStringConverter}}" TextAlignment="Center" FontSize="30" Width="75" Height="75"/>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </GridView>
    </Grid>
</Page>
